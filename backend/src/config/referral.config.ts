/**
 * Referral program (Phase 4) — a flat credit to the referrer once the
 * person they referred completes their first trip. See
 * ReferralsService.rewardReferrerForFirstTrip. A launch default, not a
 * policy carved in stone — same footing as COMMISSION_RATES.
 */
export const REFERRAL_REWARD_AMOUNT_ENV = 'REFERRAL_REWARD_AMOUNT';
export const DEFAULT_REFERRAL_REWARD_AMOUNT = 20;
