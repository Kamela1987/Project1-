services:
  photo-upscaler:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - model-weights:/app/weights
      - gfpgan-cache:/root/.cache

volumes:
  model-weights:
  gfpgan-cache:
